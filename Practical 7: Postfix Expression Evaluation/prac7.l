%{
        #include <stdio.h>
        #include "shreyas.tab.h"
%}

%%

[0-9]+  { yylval = atoi(yytext); return NUMBER; }
[-+*/]+ { return yytext[0]; }
[\t]    ;
\n              { return '\n'; }
.               { printf("%s", yytext); }

%%

int yywrap(void)
{
        return 1;
}
